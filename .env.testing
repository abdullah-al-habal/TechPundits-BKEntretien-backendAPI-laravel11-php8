# Application Settings
APP_NAME="BK Entretien"
APP_ENV=testing
APP_KEY=base64:YyqFw3DaqCxDpvXYv3LjLqtwPisXF7V5EcNaTVdQGyw=
APP_DEBUG=true
APP_URL=http://localhost
APP_VERSION=1.0.0
APP_TIMEZONE=UTC
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

# Maintenance Settings
APP_MAINTENANCE_DRIVER=file
APP_MAINTENANCE_STORE=database

# Security Settings
BCRYPT_ROUNDS=12

# Logging Settings
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database Settings
DB_CONNECTION=sqlite
DB_DATABASE=:memory:

# Session Settings
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_PATH=/
SESSION_DOMAIN=null
SESSION_SECURE_COOKIE=false

# Cache Settings
CACHE_DRIVER=database
CACHE_PREFIX=laravel_cache
CACHE_TTL=3600

# Queue Settings
QUEUE_CONNECTION=database
QUEUE_FAILED_DRIVER=database

# Broadcasting Settings
BROADCAST_DRIVER=log
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# Storage Settings
FILESYSTEM_DISK=local

# Redis Settings
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Mail Settings
MAIL_MAILER=smtp
MAIL_HOST=127.0.0.1
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# AWS Settings
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Vite Settings
VITE_APP_NAME="${APP_NAME}"

# API & Sanctum Settings
SANCTUM_STATEFUL_DOMAINS=website.com,localhost,127.0.0.1
SESSION_DOMAIN=localhost

# Monitoring & Debugging
TELESCOPE_ENABLED=true
DEBUGBAR_ENABLED=true

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_ATTEMPTS=60
RATE_LIMIT_DECAY_MINUTES=1

# Feature Flags
FEATURE_REGISTRATION=true
FEATURE_SOCIAL_LOGIN=false
FEATURE_API=true

# Additional Services
SCOUT_DRIVER=null
MEMCACHED_HOST=127.0.0.1

# External API and Redis Check Settings
EXTERNAL_API_URL=
CHECK_REDIS=false